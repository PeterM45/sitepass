node_modules
dist
coverage

# Never commit real secrets. .env.example holds empty placeholders only.
.env
.env.*
!.env.example
.dev.vars

*.log
.DS_Store

# Internal security audit report — kept local, not published (it details exploit
# mechanics for issues fixed on main; track findings via releases/advisories).
SECURITY-AUDIT.md
